# Függőségek
node_modules/
.pnpm-store/

# Build
dist/
build/
*.tsbuildinfo

# Környezeti változók (soha ne kerüljön a repóba — csak a .env.example)
.env
.env.local
.env.*.local

# Naplók
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Teszt / coverage
coverage/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Cursor — személyes / házi munkaterület (ne kerüljön a repóba)
.cursor/

# BMAD — személyes (installer-managed) beállítások; a keret + a custom team-config megy a repóba
_bmad/config.user.toml
# BMAD skills: a telepítő generálja (.claude/skills) — ne a leadott repóba
.claude/skills/
# Claude Code — gépspecifikus beállítás
.claude/settings.local.json
